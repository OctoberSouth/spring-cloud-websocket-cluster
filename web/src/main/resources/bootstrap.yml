spring:
  application:
    name: web
  redis:
    host: 127.0.0.1
  cloud:
    nacos:
      server-addr: 127.0.0.1
      config:
        file-extension: yaml
server:
  port: 8081

logging:
  level:
    com.lp.feign: debug