spring:
  application:
    #随机名字，做ws集群使用
    name: ${SpringApplicationName}
  redis:
    host: sys10-dev.juyoun.net
    password: qwe123
  cloud:
    nacos:
      server-addr: 127.0.0.1
      config:
        file-extension: yaml

#性能监控
management:
  endpoint:
    prometheus:
      enabled: true
  endpoints:
    web:
      exposure:
        include: '*'