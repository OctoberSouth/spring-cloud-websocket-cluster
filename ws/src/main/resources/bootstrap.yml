spring:
  application:
    #随机名字，做ws集群使用
    name: ${SpringApplicationName}
  #    name: ws
  redis:
    host: 127.0.0.1
  cloud:
    nacos:
      server-addr: 127.0.0.1
      config:
        file-extension: yaml

server:
  port: 9090